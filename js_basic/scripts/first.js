// 변수
// var 변수명 = 대입값;
// var : 변수 선언 키워드
// = 대입연산자 (좌 <- 우 값 대입)
var num; // num 변수 생성
num = 1; // 1을 num에 대입 (변수에 값 대입)
var num2 = 2; // 생성과 대입
// 콘솔 실행 함수
console.log(num2);
// 객체.함수(실행하는 값); -> 콘솔창(객체)에 num2 변수값(실행값)을 로그(실행 함수)로 출력
console.log(num);